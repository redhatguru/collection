---
- name: Restart postgresql
  ansible.builtin.systemd:
    name: "{{ 'postgresql-' + postgresql_version if ansible_facts['os_family'] == 'RedHat' else 'postgresql' }}"
    state: restarted
    enabled: true
